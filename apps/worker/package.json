{
  "name": "@incident/worker",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc -p ../../tsconfig.json --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply incident-training --local",
    "db:migrate:remote": "wrangler d1 migrations apply incident-training --remote"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "latest",
    "@cloudflare/sandbox": "latest",
    "@incident/observability": "workspace:*",
    "@incident/scenarios": "workspace:*",
    "@incident/shared": "workspace:*",
    "hono": "latest"
  }
}
